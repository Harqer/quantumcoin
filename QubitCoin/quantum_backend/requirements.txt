# Quantum Backend — Production Dependencies
# Core quantum SDK
qiskit>=1.2.0
qiskit-ibm-runtime>=0.30.0

# Fire Opal — Q-CTRL automated error suppression
fire-opal>=8.0.0

# qBraid — unified multi-provider quantum runtime
qbraid>=0.8.0
qbraid-qir>=0.4.0

# BlueQubit — quantum cloud access
bluequbit>=0.18.0b1

# Azure Quantum
azure-quantum>=2.0.0
qsharp>=1.0.0

# AWS Braket — access IonQ, IQM, Rigetti, QuEra, AQT via Amazon
amazon-braket-sdk>=1.80.0
qiskit-braket-provider>=0.4.0

# PennyLane (IonQ + general)
pennylane>=0.38.0
pennylane-ionq>=0.38.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# Cryptography
cryptography>=43.0.0

# Post-Quantum Signatures — NIST FIPS 204/205 (ML-DSA, SPHINCS+)
# liboqs-python auto-builds liboqs C library (needs cmake, libssl-dev)
liboqs-python>=0.15.0

# Utilities
numpy>=1.26.0
python-dotenv>=1.0.0
httpx>=0.27.0
structlog>=24.0.0
